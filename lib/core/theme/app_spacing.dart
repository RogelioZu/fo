/// Tokens de espaciado de Finding Out.
/// Valores extraídos de login.pen.
class AppSpacing {
  AppSpacing._();

  /// 4px — Espaciado mínimo
  static const double xs = 4;

  /// 8px — Gap label → input, gap interno icon-text
  static const double sm = 8;

  /// 12px — Espaciado auxiliar
  static const double ms = 12;

  /// 16px — Gap entre inputs, gap social buttons
  static const double md = 16;

  /// 20px — Gap body interests
  static const double lg20 = 20;

  /// 24px — Padding horizontal de screens, gap entre elementos
  static const double lg = 24;

  /// 32px — Espaciado grande
  static const double xl = 32;

  /// 40px — Padding bottom / padding top setup body
  static const double xxl = 40;

  /// 60px — Padding top (safe area + espacio)
  static const double xxxl = 60;

  // ─── Screen padding helpers ───

  /// Padding horizontal estándar de pantalla (24px)
  static const double screenHorizontal = lg;

  /// Padding top de pantalla (60px)
  static const double screenTop = xxxl;

  /// Padding bottom de pantalla (40px)
  static const double screenBottom = xxl;
}
